---
phase: 14-datablockbrowser
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - json-scada/src/AdminUI/src/components/DatablockBrowserPage.vue
  - json-scada/src/AdminUI/src/router/index.js
  - json-scada/src/AdminUI/src/components/DashboardPage.vue
  - json-scada/src/AdminUI/src/locales/en.json
autonomous: true
requirements: [DBBROWSER-01, DBBROWSER-02, DBBROWSER-03]

must_haves:
  truths:
    - "A 'Datablock Browser' card appears on the DashboardPage and navigates to /s7plus-datablocks"
    - "The page shows a connection dropdown that starts empty (no pre-selected connection)"
    - "Selecting a connection populates a data table with db_name and db_number columns"
    - "Each row has a 'Browse Tags' button that opens a new tab at /#/s7plus-tag-tree?db=...&connectionNumber=N"
  artifacts:
    - path: "json-scada/src/AdminUI/src/components/DatablockBrowserPage.vue"
      provides: "DatablockBrowser page component"
      min_lines: 50
    - path: "json-scada/src/AdminUI/src/router/index.js"
      provides: "Route registration for /s7plus-datablocks"
      contains: "s7plus-datablocks"
    - path: "json-scada/src/AdminUI/src/components/DashboardPage.vue"
      provides: "Dashboard shortcut card for Datablock Browser"
      contains: "datablockBrowser"
    - path: "json-scada/src/AdminUI/src/locales/en.json"
      provides: "i18n key for dashboard card"
      contains: "datablockBrowser"
  key_links:
    - from: "json-scada/src/AdminUI/src/components/DatablockBrowserPage.vue"
      to: "/Invoke/auth/listProtocolConnections"
      via: "fetch onMounted"
      pattern: "fetch.*listProtocolConnections"
    - from: "json-scada/src/AdminUI/src/components/DatablockBrowserPage.vue"
      to: "/Invoke/auth/listS7PlusDatablocks"
      via: "fetch on connection selection change"
      pattern: "fetch.*listS7PlusDatablocks"
    - from: "json-scada/src/AdminUI/src/components/DatablockBrowserPage.vue"
      to: "/#/s7plus-tag-tree"
      via: "window.open on Browse Tags click"
      pattern: "window\\.open.*s7plus-tag-tree"
    - from: "json-scada/src/AdminUI/src/router/index.js"
      to: "DatablockBrowserPage.vue"
      via: "route import and registration"
      pattern: "import DatablockBrowserPage"
---

<objective>
Create the DatablockBrowserPage — a new AdminUI page that lists all PLC datablocks for a selected connection and provides per-row navigation to the TagTreeBrowser in a new tab.

Purpose: Operators need a way to browse PLC datablocks before drilling into tag trees (Phase 15). This page is the entry point for the tag browsing workflow.
Output: Working DatablockBrowserPage accessible from the dashboard, with connection filter and new-tab navigation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-datablockbrowser/14-CONTEXT.md
@.planning/phases/14-datablockbrowser/14-RESEARCH.md
@.planning/phases/13-backend-api-datablocks-tag-endpoints/13-01-SUMMARY.md

<interfaces>
<!-- Key patterns the executor needs. Extracted from codebase. -->

From json-scada/src/AdminUI/src/router/index.js:
```javascript
import S7PlusAlarmsViewerPage from '../components/S7PlusAlarmsViewerPage.vue'
// ...
const routes = [
  // ...
  { path: '/s7plus-alarms', component: S7PlusAlarmsViewerPage },
]
const router = createRouter({
  history: createWebHashHistory(),
  routes,
})
```

From json-scada/src/AdminUI/src/components/DashboardPage.vue (shortcuts array pattern):
```javascript
import { Database, AlertTriangle, /* ... */ } from 'lucide-vue-next'

const shortcuts = ref([
  // S7Plus Alarms entry (model for new entry):
  {
    titleKey: 'dashboard.s7plusAlarms',
    icon: AlertTriangle,
    color: 'primary',
    route: '/s7plus-alarms',
  },
  // ...
])
```

From json-scada/src/AdminUI/src/locales/en.json (dashboard section):
```json
"dashboard": {
  "s7plusAlarms": "S7Plus Alarms"
}
```

Backend endpoints (Phase 13):
- GET /Invoke/auth/listProtocolConnections — returns array of {protocolConnectionNumber, name, ...}
- GET /Invoke/auth/listS7PlusDatablocks?connectionNumber=N — returns array of {db_name, db_number, ...} sorted by db_name
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DatablockBrowserPage.vue component</name>
  <files>json-scada/src/AdminUI/src/components/DatablockBrowserPage.vue</files>
  <read_first>
    - json-scada/src/AdminUI/src/components/S7PlusAlarmsViewerPage.vue (implementation model for v-data-table, v-select, fetch pattern, density="compact", class="elevation-1")
    - json-scada/src/AdminUI/src/components/DashboardPage.vue (overflowY scroll pattern in onMounted/onUnmounted)
    - .planning/phases/14-datablockbrowser/14-RESEARCH.md (complete code sketch in "Code Examples" section)
  </read_first>
  <action>
Create new file `json-scada/src/AdminUI/src/components/DatablockBrowserPage.vue` as a Vue 3 SFC with `<script setup>`.

Template structure (per D-06 — follow S7PlusAlarmsViewerPage pattern):
- `v-container fluid` wrapping everything
- `h2.mb-4` with text "Datablock Browser"
- `v-row.mb-2` > `v-col cols="12" sm="4" md="3"` containing a `v-select` for connection selection
- `v-data-table` with `density="compact"` and `class="elevation-1"` and `:items-per-page="50"`

Connection selector (per D-01 — start empty, per D-06 — v-select density="compact"):
- `v-select` with: `label="Connection"`, `:items="connectionOptions"`, `item-title="title"`, `item-value="value"`, `v-model="selectedConnection"`, `density="compact"`, `placeholder="Select a connection..."`, `clearable`
- `selectedConnection` ref initialized to `null` (D-01: no pre-selection)

Data table headers array:
```javascript
const headers = [
  { title: 'DB Name', key: 'db_name', sortable: true },
  { title: 'DB Number', key: 'db_number', sortable: true },
  { title: 'Actions', key: 'actions', sortable: false },
]
```

Actions column slot (per D-05 — explicit "Browse Tags" button per row):
- Use `#[`item.actions`]="{ item }"` template slot
- Render `v-btn` with `size="x-small"`, `variant="tonal"`, `@click="browseDatablock(item)"`, text "Browse Tags"

Script logic:
1. Imports: `ref`, `watch`, `onMounted`, `onUnmounted` from 'vue'
2. Refs: `connectionOptions = ref([])`, `selectedConnection = ref(null)`, `datablocks = ref([])`
3. `onMounted`: set `document.documentElement.style.overflowY = 'scroll'`, then fetch `GET /Invoke/auth/listProtocolConnections`, map response to `{ title: c.name, value: c.protocolConnectionNumber }` and assign to `connectionOptions.value`
4. `onUnmounted`: set `document.documentElement.style.overflowY = 'hidden'`
5. `watch(selectedConnection, ...)`: when non-null, fetch `GET /Invoke/auth/listS7PlusDatablocks?connectionNumber=${newVal}`, assign array to `datablocks.value`; when null/undefined, set `datablocks.value = []`
6. `browseDatablock(row)` function (per D-03 and D-04): construct URL as `` `/#/s7plus-tag-tree?db=${encodeURIComponent(row.db_name)}&connectionNumber=${selectedConnection.value}` `` and call `window.open(url, '_blank')`

All fetch calls use bare `/Invoke/auth/...` paths (cookie-based auth — no Authorization header per D-06).
No auto-refresh timer (datablocks are static after driver startup per D-06).
Wrap all fetch calls in try/catch with `console.warn` on error.
  </action>
  <verify>
    <automated>cd json-scada/src/AdminUI && npx vue-tsc --noEmit 2>/dev/null; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File `json-scada/src/AdminUI/src/components/DatablockBrowserPage.vue` exists and has more than 50 lines
    - File contains `v-data-table` with `density="compact"` and `class="elevation-1"`
    - File contains `v-select` with `density="compact"` and `v-model="selectedConnection"`
    - File contains `selectedConnection = ref(null)` (D-01: starts empty)
    - File contains `fetch('/Invoke/auth/listProtocolConnections')` (connection dropdown population)
    - File contains `fetch` call with `listS7PlusDatablocks?connectionNumber=` (datablock fetch)
    - File contains `window.open` with `s7plus-tag-tree` and `encodeURIComponent` (D-03, D-04)
    - File contains `Browse Tags` button text (D-05)
    - File does NOT contain `setInterval` (no auto-refresh per D-06)
    - `npm run build` in AdminUI directory exits with code 0
  </acceptance_criteria>
  <done>DatablockBrowserPage.vue created with connection dropdown, datablock table, and per-row Browse Tags button that opens new tab. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Wire DatablockBrowserPage into router, dashboard, and i18n</name>
  <files>
    json-scada/src/AdminUI/src/router/index.js,
    json-scada/src/AdminUI/src/components/DashboardPage.vue,
    json-scada/src/AdminUI/src/locales/en.json
  </files>
  <read_first>
    - json-scada/src/AdminUI/src/router/index.js (current routes array — add after s7plus-alarms entry)
    - json-scada/src/AdminUI/src/components/DashboardPage.vue (shortcuts array — add after s7plusAlarms entry; Database icon already imported at line 53)
    - json-scada/src/AdminUI/src/locales/en.json (dashboard section — add after s7plusAlarms key)
  </read_first>
  <action>
**router/index.js** — Add two lines:
1. Import at top (after the S7PlusAlarmsViewerPage import on line 15):
   ```javascript
   import DatablockBrowserPage from '../components/DatablockBrowserPage.vue'
   ```
2. Route entry in the `routes` array (after the `{ path: '/s7plus-alarms', ... }` entry on line 44):
   ```javascript
   { path: '/s7plus-datablocks', component: DatablockBrowserPage },
   ```

**DashboardPage.vue** — Add one shortcut entry in the `shortcuts` ref array, immediately AFTER the s7plusAlarms entry (after line 92). Use `Database` icon which is already imported at line 53 — do NOT add a new import:
```javascript
{
  titleKey: 'dashboard.datablockBrowser',
  icon: Database,
  color: 'primary',
  route: '/s7plus-datablocks',
},
```

**en.json** — Add one key under the `"dashboard"` object, after the `"s7plusAlarms": "S7Plus Alarms"` line (line 39):
```json
"datablockBrowser": "Datablock Browser"
```
Ensure proper comma after the preceding `"s7plusAlarms": "S7Plus Alarms"` entry.
  </action>
  <verify>
    <automated>cd json-scada/src/AdminUI && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `json-scada/src/AdminUI/src/router/index.js` contains `import DatablockBrowserPage from '../components/DatablockBrowserPage.vue'`
    - `json-scada/src/AdminUI/src/router/index.js` contains `path: '/s7plus-datablocks'`
    - `json-scada/src/AdminUI/src/components/DashboardPage.vue` contains `titleKey: 'dashboard.datablockBrowser'`
    - `json-scada/src/AdminUI/src/components/DashboardPage.vue` contains `route: '/s7plus-datablocks'`
    - `json-scada/src/AdminUI/src/components/DashboardPage.vue` contains `icon: Database` in the new shortcut entry
    - `json-scada/src/AdminUI/src/locales/en.json` contains `"datablockBrowser": "Datablock Browser"`
    - `npm run build` in AdminUI directory exits with code 0
  </acceptance_criteria>
  <done>DatablockBrowserPage is routable at /s7plus-datablocks, visible on the dashboard as "Datablock Browser" card, and i18n key is registered. Full build succeeds.</done>
</task>

</tasks>

<verification>
1. `cd json-scada/src/AdminUI && npm run build` exits with code 0
2. `grep -c 'DatablockBrowserPage' json-scada/src/AdminUI/src/router/index.js` returns 2 (import + route)
3. `grep -c 'datablockBrowser' json-scada/src/AdminUI/src/locales/en.json` returns 1
4. `grep -c 'datablockBrowser' json-scada/src/AdminUI/src/components/DashboardPage.vue` returns 1
5. `grep 'window.open' json-scada/src/AdminUI/src/components/DatablockBrowserPage.vue` shows s7plus-tag-tree URL
6. `grep 'listProtocolConnections' json-scada/src/AdminUI/src/components/DatablockBrowserPage.vue` returns a match
</verification>

<success_criteria>
- AdminUI builds without errors
- DatablockBrowserPage.vue exists with connection dropdown (starts empty), datablock data table, and per-row Browse Tags button
- Route /s7plus-datablocks registered in router
- Dashboard card "Datablock Browser" navigates to the page
- Browse Tags button constructs correct hash URL with encodeURIComponent and opens new tab
</success_criteria>

<output>
After completion, create `.planning/phases/14-datablockbrowser/14-01-SUMMARY.md`
</output>
