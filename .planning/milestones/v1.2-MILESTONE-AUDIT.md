---
milestone: v1.2
audited: 2026-03-24T14:00:00Z
status: tech_debt
scores:
  requirements: 13/14
  phases: 4/4
  integration: 4/4
  flows: 4/4
gaps: []
tech_debt:
  - phase: 05-driver-relationid-fields
    items:
      - "Build verification (dotnet build) is human-gated — dotnet SDK unavailable in automated environment; fields are syntactically correct and match established patterns"
      - "Live document verification (trigger alarm, inspect MongoDB) requires PLCSIM or hardware — cannot automate in PoC context"
  - phase: 06-driver-startup-db-name-map
    items:
      - "Runtime originDbName accuracy requires PLCSIM — human verification recommended before production use"
      - "Browse failure path (GetListOfDatablocks) untested against a PLC that does not support the call"
  - phase: 07-backend-delete-endpoint-id-exposure
    items:
      - "Auth guard HTTP response code (401 vs 403 vs hang on no-token path) requires live server test — pre-existing middleware ambiguity in json-scada, not a Phase 7 regression"
  - phase: 08-frontend-delete-buttons-origin-columns
    items:
      - "Browser UI verification already APPROVED by user 2026-03-24; no outstanding human items"
  - phase: all
    items:
      - "No automated test suite exists for any phase — all 4 VALIDATION.md files are draft status, nyquist_compliant: false"
      - "Spinner resolution edge case in fetchAlarms: stillPending.add(id) missing — spinner always clears after one 5s poll regardless of ackState (pre-existing from Phase 4, unobservable in normal <1s ack scenario)"
      - "connectionId vs connectionNumber naming inconsistency (MongoDB field name vs API body key)"
      - "LCID hardcoded to 1033 — English only"
      - "No auto-resubscribe after alarm subscription failure mid-session"
intentionally_dropped:
  - id: ORIGIN-08
    reason: "D-02 locked decision: raw 64-bit RelationId column adds no operator value; decision made in 08-CONTEXT.md and documented in PLAN frontmatter and SUMMARY"
notes:
  - "Phase 5 VERIFICATION.md status is human_needed (2/3 automated) but all code-verifiable truths are satisfied — build requires dotnet, runtime requires PLCSIM"
  - "Phase 7 VERIFICATION.md status is human_needed (5/6 automated) — auth guard runtime HTTP code has pre-existing ambiguity in json-scada middleware; document-not-deleted requirement is met at the code level"
  - "DELETE-03 filter-object bulk delete path has no Phase 8 UI caller — intentional design; Phase 8 bulk delete uses the ids-array path for both single-row and filtered operations"
---

# v1.2 Milestone Audit — Alarm Origin & Cleanup

**Audited:** 2026-03-24
**Status:** ⚡ TECH DEBT (no blockers — all requirements satisfied; accumulated deferred items)
**Score:** 13/14 requirements satisfied (1 intentionally dropped per D-02)

---

## Requirements Coverage

| REQ-ID | Phase | VERIFICATION.md | SUMMARY frontmatter | REQUIREMENTS.md | Final Status |
|--------|-------|----------------|---------------------|-----------------|--------------|
| ORIGIN-01 | 5 | SATISFIED | listed | [x] | ✅ satisfied |
| ORIGIN-02 | 5 | SATISFIED | listed | [x] | ✅ satisfied |
| ORIGIN-03 | 6 | SATISFIED | — | [x] | ✅ satisfied |
| ORIGIN-04 | 6 | SATISFIED | — | [x] | ✅ satisfied |
| ORIGIN-05 | 7 | SATISFIED | — | [x] | ✅ satisfied |
| ORIGIN-06 | 8 | SATISFIED | — | [ ] (stale) | ✅ satisfied (checkbox stale) |
| ORIGIN-07 | 8 | SATISFIED | — | [ ] (stale) | ✅ satisfied (checkbox stale) |
| ORIGIN-08 | 8 | SKIPPED (D-02) | — | [ ] | ⛔ intentionally dropped |
| DELETE-01 | 7 | SATISFIED | — | [x] | ✅ satisfied |
| DELETE-02 | 7 | SATISFIED | — | [x] | ✅ satisfied |
| DELETE-03 | 7 | SATISFIED | — | [x] | ✅ satisfied |
| DELETE-04 | 8 | SATISFIED | — | [ ] (stale) | ✅ satisfied (checkbox stale) |
| DELETE-05 | 8 | SATISFIED | — | [ ] (stale) | ✅ satisfied (checkbox stale) |
| DELETE-06 | 8 | SATISFIED | — | [ ] (stale) | ✅ satisfied (checkbox stale) |

**Note:** REQUIREMENTS.md checkboxes for ORIGIN-06, ORIGIN-07, DELETE-04, DELETE-05, DELETE-06 are stale — they were not updated after Phase 8 execution. All five are confirmed satisfied by VERIFICATION.md evidence and user approval (2026-03-24). ORIGIN-08 was intentionally dropped per D-02 and is not a gap.

---

## Phase Verification Summary

| Phase | VERIFICATION Status | Score | Human Items |
|-------|-------------------|-------|-------------|
| 5 — Driver RelationId Fields | human_needed | 2/3 automated | dotnet build + live PLC |
| 6 — Driver Startup DB Name Map | passed | 4/4 | runtime doc check (PLCSIM) |
| 7 — Backend Delete + _id Exposure | human_needed | 5/6 automated | auth guard HTTP code |
| 8 — Frontend Delete + Origin Columns | passed | 7/7 | browser check (APPROVED ✓) |

All phases verified at the code level. Human-gated items are inherent to the PoC environment (no live PLC in CI, no running server in CI). Phase 8 browser verification was already approved by the user on 2026-03-24.

---

## Cross-Phase Integration

**4/4 E2E flows complete. 0 broken connections. 0 orphaned exports.**

### Flow 1: Alarm written with origin fields (Phases 5 → 6 → MongoDB)
```
AlarmThread.cs:BuildAlarmDocument()
  line ~235: uint relationId = (uint)(dai.CpuAlarmId >> 32)       [Phase 5]
  line ~236: uint dbNumber   = relationId & 0xFFFF                 [Phase 5]
  line ~256: { "relationId", new BsonInt64((long)relationId) }     [Phase 5]
  line ~257: { "dbNumber",   (int)dbNumber }                       [Phase 5]
  line ~258: { "originDbName", srv.RelationIdNameMap.TryGetValue(...) }  [Phase 6]
  → InsertOneAsync() → s7plusAlarmEvents
```
Status: ✅ COMPLETE

### Flow 2: Origin fields surfaced through API (Phases 5+6 → 7 → 8)
```
s7plusAlarmEvents (has _id, relationId, dbNumber, originDbName)
  → listS7PlusAlarms: find({}) — NO { _id: 0 } projection     [Phase 7]
  → fetchAlarms() → alarms.value
  → headers: key:'originDbName', key:'dbNumber'               [Phase 8]
  → v-data-table renders Origin DB Name + DB Number columns
```
Status: ✅ COMPLETE

### Flow 3: Per-row delete (Phase 7 → 8)
```
listS7PlusAlarms._id (24-hex string via mongo driver v7)
  → handleDeleteRow → executeDeleteRow
  → alarms.value.splice(idx, 1)                  [optimistic removal]
  → POST /deleteS7PlusAlarms { ids: [item._id] }
  → new ObjectId(id) → deleteMany({ _id: { $in: } })
  → 204 No Content
```
Status: ✅ COMPLETE

### Flow 4: Bulk filtered delete (Phase 8)
```
filteredAlarms.value.map(a => a._id)  → ids[]
  → POST /deleteS7PlusAlarms { ids: [...] }
  → same ids-array branch → deleteMany($in)
  → 204 No Content
```
Status: ✅ COMPLETE

---

## Integration Findings (Non-blocking)

| ID | Severity | Description | Requirement Impact |
|----|----------|-------------|-------------------|
| I-01 | LOW | `listS7PlusAlarms` error paths return HTTP 200 with `{ error: ... }` instead of 4xx/5xx; Vue silently ignores non-array response; no visible error UX on DB disconnect | None — no requirement specifies error UX for list endpoint |
| I-02 | INFO | `listS7PlusAlarms` registered with `app.use` (all HTTP methods) instead of `app.get` — pre-existing pattern in json-scada codebase, not introduced by this milestone | None |
| I-03 | INFO | DELETE-03 filter-object bulk delete backend path has no Phase 8 UI caller; Phase 8 uses `ids` array for both single and bulk operations — intentional design | None — DELETE-03 is fully implemented; the UI never needs the filter path given it has explicit `_id` values |

---

## Nyquist Compliance

| Phase | VALIDATION.md | nyquist_compliant | wave_0_complete | Action |
|-------|---------------|-------------------|-----------------|--------|
| 5 | exists (draft) | false | false | `/gsd:validate-phase 5` |
| 6 | exists (draft) | false | false | `/gsd:validate-phase 6` |
| 7 | exists (draft) | false | false | `/gsd:validate-phase 7` |
| 8 | exists (draft) | false | false | `/gsd:validate-phase 8` |

All 4 phases have VALIDATION.md files in draft status. None are Nyquist-compliant. This is consistent with the project constraint ("No automated tests — PoC" in PROJECT.md Known Technical Debt). Running `/gsd:validate-phase` for any phase would generate tests, but this is a PoC and no test framework exists.

---

## Known Technical Debt (Carried Forward)

From phase verifications + project context:

| Item | Phase | Severity | Notes |
|------|-------|----------|-------|
| No automated test suite | All | LOW (PoC) | Consistent with stated PoC constraints |
| Dotnet build verification human-gated | 5 | LOW | Fields are correct C#; failure unexpected |
| Live alarm origin doc verification | 5, 6 | LOW | Requires PLCSIM — manual check recommended |
| Auth guard HTTP response code ambiguity | 7 | LOW | Pre-existing json-scada middleware behavior |
| Spinner resolution edge case (stillPending) | 4 (carried) | LOW | Spinner clears after 1 poll regardless; unobservable at normal <1s ack |
| connectionId vs connectionNumber naming | Pre-existing | INFO | MongoDB field vs API body key mismatch |
| LCID hardcoded to 1033 | Pre-existing | INFO | English only |
| No auto-resubscribe on alarm subscription failure | Pre-existing | LOW | Single connection PoC |

---

## Verdict

**✅ No critical blockers. 13/14 requirements satisfied. 1 intentionally dropped.**

All cross-phase data flows are correctly wired. The integration checker found no broken connections. Tech debt is accumulated from the PoC approach (no automated tests, human-gated live PLC checks) — none of it blocks milestone completion.

`/gsd:complete-milestone v1.2` is safe to proceed.
