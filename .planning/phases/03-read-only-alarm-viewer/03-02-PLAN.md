---
phase: 03-read-only-alarm-viewer
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - json-scada/src/AdminUI/src/router/index.js
  - json-scada/src/AdminUI/src/components/DashboardPage.vue
  - json-scada/src/AdminUI/src/locales/en.json
  - json-scada/src/AdminUI/src/locales/ar.json
  - json-scada/src/AdminUI/src/locales/de.json
  - json-scada/src/AdminUI/src/locales/es.json
  - json-scada/src/AdminUI/src/locales/fa.json
  - json-scada/src/AdminUI/src/locales/fr.json
  - json-scada/src/AdminUI/src/locales/it.json
  - json-scada/src/AdminUI/src/locales/ja.json
  - json-scada/src/AdminUI/src/locales/ps.json
  - json-scada/src/AdminUI/src/locales/pt.json
  - json-scada/src/AdminUI/src/locales/ru.json
  - json-scada/src/AdminUI/src/locales/uk.json
  - json-scada/src/AdminUI/src/locales/zh.json
autonomous: false
requirements: [VIEW-01]

must_haves:
  truths:
    - "Navigating to /#/s7plus-alarms loads S7PlusAlarmsViewerPage component"
    - "Dashboard shows an S7Plus Alarms tile that navigates to /s7plus-alarms on click"
    - "Dashboard tile label displays translated text from i18n key dashboard.s7plusAlarms"
    - "Existing /alarms-viewer route and tile are unchanged"
  artifacts:
    - path: "json-scada/src/AdminUI/src/router/index.js"
      provides: "Route entry for /s7plus-alarms"
      contains: "s7plus-alarms"
    - path: "json-scada/src/AdminUI/src/components/DashboardPage.vue"
      provides: "Dashboard tile shortcut entry"
      contains: "s7plusAlarms"
    - path: "json-scada/src/AdminUI/src/locales/en.json"
      provides: "English i18n key"
      contains: "s7plusAlarms"
  key_links:
    - from: "router/index.js"
      to: "S7PlusAlarmsViewerPage.vue"
      via: "import + route entry"
      pattern: "import S7PlusAlarmsViewerPage"
    - from: "DashboardPage.vue"
      to: "/s7plus-alarms"
      via: "shortcuts array route property"
      pattern: "route.*s7plus-alarms"
---

<objective>
Wire the S7Plus Alarms Viewer into AdminUI navigation: register the Vue Router route, add the Dashboard tile, and add i18n keys to all 13 locale files.

Purpose: Completes VIEW-01 by making the viewer page navigable from the Dashboard and directly via URL. Without this plan, the component from Plan 01 exists but is unreachable.
Output: Route registered, Dashboard tile visible, i18n keys in all locales.
</objective>

<execution_context>
@C:/Users/tnielen/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/tnielen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-read-only-alarm-viewer/03-CONTEXT.md
@.planning/phases/03-read-only-alarm-viewer/03-RESEARCH.md
@.planning/phases/03-read-only-alarm-viewer/03-01-SUMMARY.md

<interfaces>
<!-- Exact current state of files being modified. Executor MUST read these. -->

From router/index.js (full file, 51 lines):
- Lines 1-14: Eager imports of all page components from '../components/'
- Lines 16-43: `const routes = [...]` array with `{ path, component }` entries
- Line 11: `import AlarmsViewerPage from '../components/AlarmsViewerPage.vue'` (existing — must NOT be removed)
- Line 26: `{ path: '/alarms-viewer', component: AlarmsViewerPage }` (existing — must NOT be removed)
- Line 42: `{ path: '/svg-edit', component: SVGEditPage }` (last entry before array close on line 43)

From DashboardPage.vue (script setup, lines 41-195):
- Lines 44-56: lucide-vue-next icon imports (Monitor, Bell, Table, Calendar, FileText, UserCog, HelpCircle, BarChart, Database, PenTool, Code)
- Lines 69-158: `const shortcuts = ref([...])` array — each entry has `{ titleKey, icon, color, route, page?, target? }`
- Line 79-85: Existing alarmsViewer entry with `page` and `target` keys (external link pattern)
- Line 150-157: Last shortcut entry (About) before array close on line 158
- CRITICAL: New S7Plus tile must NOT have `page` or `target` keys (internal SPA route only)

From en.json dashboard section (lines 25-39):
- `"dashboard": { ... "svgedit": "SVG Editor" }` — keys are camelCase
- New key: `"s7plusAlarms": "S7Plus Alarms"` to be added after "svgedit"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register route, add Dashboard tile, and add i18n keys</name>
  <files>
    json-scada/src/AdminUI/src/router/index.js,
    json-scada/src/AdminUI/src/components/DashboardPage.vue,
    json-scada/src/AdminUI/src/locales/en.json,
    json-scada/src/AdminUI/src/locales/ar.json,
    json-scada/src/AdminUI/src/locales/de.json,
    json-scada/src/AdminUI/src/locales/es.json,
    json-scada/src/AdminUI/src/locales/fa.json,
    json-scada/src/AdminUI/src/locales/fr.json,
    json-scada/src/AdminUI/src/locales/it.json,
    json-scada/src/AdminUI/src/locales/ja.json,
    json-scada/src/AdminUI/src/locales/ps.json,
    json-scada/src/AdminUI/src/locales/pt.json,
    json-scada/src/AdminUI/src/locales/ru.json,
    json-scada/src/AdminUI/src/locales/uk.json,
    json-scada/src/AdminUI/src/locales/zh.json
  </files>
  <read_first>
    - json-scada/src/AdminUI/src/router/index.js (full file — see current imports and routes array)
    - json-scada/src/AdminUI/src/components/DashboardPage.vue (full file — see lucide imports, shortcuts array, handleCardClick)
    - json-scada/src/AdminUI/src/locales/en.json (full file — see dashboard key section)
    - json-scada/src/AdminUI/src/locales/ar.json (first 40 lines — confirm dashboard section structure matches en.json)
    - json-scada/src/AdminUI/src/components/S7PlusAlarmsViewerPage.vue (confirm it exists from Plan 01)
  </read_first>
  <action>
**1. router/index.js — Add import and route entry**

Add import line after line 14 (after the last existing import):
```javascript
import S7PlusAlarmsViewerPage from '../components/S7PlusAlarmsViewerPage.vue'
```

Add route entry inside the `routes` array, after the `{ path: '/svg-edit', component: SVGEditPage }` entry (line 42) and before the closing `]` (line 43):
```javascript
  { path: '/s7plus-alarms', component: S7PlusAlarmsViewerPage },
```

Do NOT modify or remove the existing `AlarmsViewerPage` import (line 11) or its route entry (lines 25-27).

**2. DashboardPage.vue — Add lucide icon import and shortcuts entry**

Add `AlertTriangle` to the lucide-vue-next import destructuring (line 44-56). The updated import should include `AlertTriangle` after the existing imports:
```javascript
import {
    Monitor,
    Bell,
    Table,
    Calendar,
    FileText,
    UserCog,
    HelpCircle,
    BarChart,
    Database,
    PenTool,
    Code,
    AlertTriangle,
  } from 'lucide-vue-next'
```

Add a new shortcut entry in the `shortcuts` ref array. Insert it AFTER the existing `alarmsViewer` entry (line 85) and BEFORE the `tabularViewer` entry (line 86), so it appears next to the existing alarm viewer tile:
```javascript
    {
      titleKey: 'dashboard.s7plusAlarms',
      icon: AlertTriangle,
      color: 'primary',
      route: '/s7plus-alarms',
    },
```

CRITICAL: Do NOT add `page` or `target` keys. The absence of `page` prevents the external-link `v-btn` from rendering (see template line 26: `v-if="shortcut.page"`). This is an internal SPA route only.

Do NOT modify the existing alarmsViewer entry (lines 78-85).

**3. i18n — Add `dashboard.s7plusAlarms` key to all 13 locale files**

In each locale file, find the `"dashboard"` object and add:
```json
"s7plusAlarms": "S7Plus Alarms"
```

Place it after the last existing dashboard key (`"svgedit": "SVG Editor"` in en.json, same relative position in others). Add a comma after the preceding entry if needed.

Files to update (all 13):
- `en.json`: `"s7plusAlarms": "S7Plus Alarms"`
- `ar.json`: `"s7plusAlarms": "S7Plus Alarms"`
- `de.json`: `"s7plusAlarms": "S7Plus Alarms"`
- `es.json`: `"s7plusAlarms": "S7Plus Alarms"`
- `fa.json`: `"s7plusAlarms": "S7Plus Alarms"`
- `fr.json`: `"s7plusAlarms": "S7Plus Alarms"`
- `it.json`: `"s7plusAlarms": "S7Plus Alarms"`
- `ja.json`: `"s7plusAlarms": "S7Plus Alarms"`
- `ps.json`: `"s7plusAlarms": "S7Plus Alarms"`
- `pt.json`: `"s7plusAlarms": "S7Plus Alarms"`
- `ru.json`: `"s7plusAlarms": "S7Plus Alarms"`
- `uk.json`: `"s7plusAlarms": "S7Plus Alarms"`
- `zh.json`: `"s7plusAlarms": "S7Plus Alarms"`

Use the English string as the value in all files (translators update later).
  </action>
  <verify>
    <automated>grep -c "S7PlusAlarmsViewerPage" json-scada/src/AdminUI/src/router/index.js && grep -c "s7plusAlarms" json-scada/src/AdminUI/src/components/DashboardPage.vue && grep -c "s7plusAlarms" json-scada/src/AdminUI/src/locales/en.json && grep -c "s7plusAlarms" json-scada/src/AdminUI/src/locales/zh.json</automated>
  </verify>
  <acceptance_criteria>
    - router/index.js contains `import S7PlusAlarmsViewerPage from '../components/S7PlusAlarmsViewerPage.vue'`
    - router/index.js contains `{ path: '/s7plus-alarms', component: S7PlusAlarmsViewerPage }`
    - router/index.js still contains `import AlarmsViewerPage` (existing page preserved)
    - router/index.js still contains `path: '/alarms-viewer'` (existing route preserved)
    - DashboardPage.vue contains `AlertTriangle` in lucide import
    - DashboardPage.vue contains `titleKey: 'dashboard.s7plusAlarms'`
    - DashboardPage.vue contains `route: '/s7plus-alarms'`
    - DashboardPage.vue shortcut entry does NOT contain `page:` key (grep for `page.*s7plus` returns 0 matches)
    - DashboardPage.vue still contains `titleKey: 'dashboard.alarmsViewer'` (existing tile preserved)
    - All 13 locale files contain `"s7plusAlarms": "S7Plus Alarms"`
  </acceptance_criteria>
  <done>Route /s7plus-alarms registered, Dashboard tile added alongside existing Alarms Viewer tile, i18n key present in all 13 locales</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete alarm viewer navigation and display</name>
  <files>json-scada/src/AdminUI/src/components/S7PlusAlarmsViewerPage.vue</files>
  <action>
Human verifies the complete S7Plus Alarms Viewer works end-to-end in the browser. This covers all phase requirements (VIEW-01, VIEW-02, VIEW-04, VIEW-05, VIEW-06) from both Plans 01 and 02.

Steps:
1. Start AdminUI dev server if not running: `cd json-scada/src/AdminUI && npm run dev`
2. Start server_realtime_auth if not running: `cd json-scada/src/server_realtime_auth && node index.js`
3. Open browser to AdminUI Dashboard
4. Verify S7Plus Alarms tile appears with AlertTriangle icon (VIEW-01)
5. Verify existing Alarms Viewer tile is unchanged (VIEW-01)
6. Click S7Plus Alarms tile, verify navigation to /#/s7plus-alarms (VIEW-01)
7. Verify table shows 11 columns: Source, Date, Time, Status, Acknowledge, Alarm class name, Event text, ID, Additional text 1-3 (VIEW-02)
8. Verify Status chips are colored red/green and Acknowledge icons show mdi-check/mdi-close (VIEW-02)
9. Wait 5 seconds, verify table auto-refreshes (VIEW-04)
10. Test Status filter dropdown (VIEW-05)
11. Test Alarm class filter dropdown (VIEW-06)
12. Navigate to /#/alarms-viewer, verify existing viewer unchanged (VIEW-01)
  </action>
  <verify>Human confirms all verification steps pass</verify>
  <done>Human approves the complete alarm viewer by typing "approved"</done>
</task>

</tasks>

<verification>
- grep for `S7PlusAlarmsViewerPage` in router/index.js confirms import and route registration
- grep for `s7plusAlarms` in DashboardPage.vue confirms tile entry
- grep for `s7plusAlarms` across all 13 locale files confirms i18n coverage
- grep for `AlarmsViewerPage` in router/index.js confirms existing viewer preserved
- Human verification confirms end-to-end navigation flow works in browser
</verification>

<success_criteria>
1. /#/s7plus-alarms loads the S7Plus Alarms Viewer page (VIEW-01)
2. Dashboard shows S7Plus Alarms tile that navigates to the viewer
3. Existing /alarms-viewer route and Dashboard tile are unchanged
4. All 13 locale files contain the dashboard.s7plusAlarms i18n key
5. Human verification confirms the complete viewer is functional
</success_criteria>

<output>
After completion, create `.planning/phases/03-read-only-alarm-viewer/03-02-SUMMARY.md`
</output>
