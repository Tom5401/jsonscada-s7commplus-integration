---
phase: quick
plan: 260330-m1y
type: execute
wave: 1
depends_on: []
files_modified:
  - json-scada/src/AdminUI/src/components/TagTreeBrowserPage.vue
autonomous: false
requirements: [QUICK-260330-m1y]
must_haves:
  truths:
    - "Leaf rows have visible horizontal dividers between them"
    - "Leaf row data (Type, Value, Timestamp, Address, Actions) is column-aligned across all visible leaves"
    - "Column headers are visible above the leaf data columns"
    - "Folder/non-leaf rows still render normally without column data"
    - "Tree expand/collapse and value refresh still work correctly"
  artifacts:
    - path: "json-scada/src/AdminUI/src/components/TagTreeBrowserPage.vue"
      provides: "Column-aligned leaf rows with headers and dividers"
  key_links:
    - from: "TagTreeBrowserPage.vue CSS grid"
      to: "#append slot content"
      via: "CSS classes on leaf data elements"
      pattern: "grid-template-columns|leaf-columns"
---

<objective>
Add row dividers and column headers to TagTreeBrowserPage leaf rows so that the inline tag data (Type, Value, Timestamp, Address, Modify button) is visually structured in aligned columns with clear row separation.

Purpose: Currently leaf data is a flat inline string of chips/text with no visual alignment. When many leaves are expanded, it is hard to scan values across rows. Column alignment and dividers make the data scannable like a table while preserving the tree structure.

Output: Updated TagTreeBrowserPage.vue with CSS-based column layout in the #append slot and a sticky column header row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@json-scada/src/AdminUI/src/components/TagTreeBrowserPage.vue
</context>

<interfaces>
<!-- Current #append slot structure (lines 20-34): -->
<!-- Each leaf gets: v-chip (type), code (value), span (timestamp), span (address), v-btn (modify) -->
<!-- These are inline elements with ml-* spacing classes, no fixed widths -->

<!-- Vuetify v-treeview structure: each row is a .v-treeview-item with nested .v-list-item -->
<!-- The #append slot content renders inside the list item, after the title text -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add column-aligned layout and row dividers to leaf rows</name>
  <files>json-scada/src/AdminUI/src/components/TagTreeBrowserPage.vue</files>
  <action>
Modify TagTreeBrowserPage.vue to add structured columns and row dividers:

**1. Add a column header bar above the v-treeview.**

Insert a header row div between the h2 and the v-treeview. This div should:
- Use CSS grid with the same column template as the leaf data rows
- Show labels: Type, Value, Timestamp, Address, Actions
- Be styled with a bottom border, subtle background (e.g. grey-lighten-4), and monospace/caption text
- Be right-aligned to match the #append slot position. Use `margin-left: auto` and a fixed total width matching the grid columns (e.g. 720px total), OR use padding-left to approximate the indentation of typical leaf nodes. A practical approach: place the header inside a div with `display: flex; justify-content: flex-end;` so it aligns to the right edge just like the append slot content.

**2. Restructure the #append slot content into a CSS grid.**

Replace the current inline layout in the `#append` slot (lines 21-33) with a wrapper div that uses CSS grid:

```html
<div v-if="item.isLeaf" class="leaf-columns">
  <span class="leaf-col-type"><v-chip size="x-small">{{ item.type }}</v-chip></span>
  <span class="leaf-col-value"><code class="text-caption">{{ formatLeafValue(item) }}</code></span>
  <span class="leaf-col-timestamp text-caption text-medium-emphasis" style="font-family: monospace;">{{ formatSourceTime(item.timeTagAtSource) }}</span>
  <span class="leaf-col-address text-caption text-medium-emphasis font-weight-light" style="font-family: monospace;">{{ item.address }}</span>
  <span class="leaf-col-actions">
    <v-btn v-if="item.commandOfSupervised !== 0" variant="outlined" size="x-small" @click.stop="openWriteDialog(item)">Modify</v-btn>
  </span>
</div>
```

**3. Add a `<style scoped>` block** (the component currently has no style block) with:

```css
.leaf-columns {
  display: grid;
  grid-template-columns: 80px 120px 220px 220px 80px;
  gap: 4px;
  align-items: center;
  min-width: 720px;
}

.leaf-col-type,
.leaf-col-value,
.leaf-col-timestamp,
.leaf-col-address,
.leaf-col-actions {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.column-header-row {
  display: grid;
  grid-template-columns: 80px 120px 220px 220px 80px;
  gap: 4px;
  min-width: 720px;
  padding: 4px 0;
  font-size: 0.75rem;
  font-weight: 600;
  color: rgba(0,0,0,0.6);
  border-bottom: 1px solid rgba(0,0,0,0.12);
}
```

**4. Add row dividers** using a Vuetify treeview CSS override (scoped with deep selector):

```css
:deep(.v-treeview-item) {
  border-bottom: 1px solid rgba(0,0,0,0.06);
}
```

This adds subtle dividers between ALL tree rows (both folders and leaves). The divider is light enough to not distract on folder rows but provides clear row separation on leaf rows.

**5. Header alignment approach:** Wrap the column header in a flex container that pushes it to the right side, matching where the #append slot renders. Place this just above the v-treeview:

```html
<div class="d-flex justify-end mb-1">
  <div class="column-header-row">
    <span>Type</span>
    <span>Value</span>
    <span>Timestamp</span>
    <span>Address</span>
    <span>Actions</span>
  </div>
</div>
```

IMPORTANT: Do NOT change any script logic -- only template structure and CSS. All existing functionality (expand/collapse, load-children, refresh, write dialog) must remain intact.
  </action>
  <verify>
    <automated>cd json-scada/src/AdminUI && npx vue-tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
    - Leaf rows display data in 5 aligned columns (Type, Value, Timestamp, Address, Actions)
    - Column headers visible above the treeview, right-aligned to match leaf data position
    - Subtle horizontal dividers between all tree rows
    - All existing functionality preserved (expand, collapse, refresh, modify button)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of column layout and dividers</name>
  <files>json-scada/src/AdminUI/src/components/TagTreeBrowserPage.vue</files>
  <action>
Human verifies the visual result in browser:
1. Navigate to the Tag Tree Browser page for any datablock connection
2. Expand a folder node to reveal leaf tags
3. Verify: Column headers (Type, Value, Timestamp, Address, Actions) are visible above the data
4. Verify: Leaf data is aligned in columns across all visible rows
5. Verify: Row dividers (subtle horizontal lines) separate each tree item
6. Verify: Folder rows still look normal (no broken layout)
7. Verify: Expand/collapse still works, values still refresh every 5s, Modify button still opens dialog

Resume signal: Type "approved" or describe any visual/alignment issues to fix
  </action>
  <verify>Human visual confirmation</verify>
  <done>User approves the column layout, row dividers, and header alignment</done>
</task>

</tasks>

<verification>
- vue-tsc type check passes without errors
- Visual inspection confirms column alignment and row dividers
- Existing tree interactions (expand, collapse, refresh, modify) unaffected
</verification>

<success_criteria>
- Leaf row data is visually structured in aligned columns
- Column headers label each data column
- Row dividers provide clear visual separation between tree items
- No regression in tree functionality
</success_criteria>

<output>
After completion, create `.planning/quick/260330-m1y-add-row-dividers-and-column-headers-to-t/260330-m1y-SUMMARY.md`
</output>
