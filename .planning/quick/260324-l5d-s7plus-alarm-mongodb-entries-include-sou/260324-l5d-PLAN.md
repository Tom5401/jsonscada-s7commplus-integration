---
phase: quick-260324-l5d
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - json-scada/src/S7CommPlusClient/AlarmThread.cs
  - json-scada/src/AdminUI/src/components/S7PlusAlarmsViewerPage.vue
autonomous: true
requirements: [connectionName-in-mongo, viewer-reads-connectionName, cleanup-fetchConnectionNames]
must_haves:
  truths:
    - "Every new alarm document written to MongoDB contains a connectionName field"
    - "Alarms Viewer Source column displays connectionName from the alarm document directly"
    - "No secondary API call to listProtocolConnections on page load"
    - "Old records without connectionName fall back to displaying raw connectionId number"
  artifacts:
    - path: "json-scada/src/S7CommPlusClient/AlarmThread.cs"
      provides: "connectionName field in BuildAlarmDocument"
      contains: "connectionName"
    - path: "json-scada/src/AdminUI/src/components/S7PlusAlarmsViewerPage.vue"
      provides: "Direct connectionName display, no fetchConnectionNames"
  key_links:
    - from: "AlarmThread.cs BuildAlarmDocument"
      to: "MongoDB s7PlusAlarms collection"
      via: "BsonDocument field connectionName"
    - from: "S7PlusAlarmsViewerPage.vue template"
      to: "alarm document connectionName"
      via: "item.connectionName || item.connectionId"
---

<objective>
Add `connectionName` field to S7Plus alarm MongoDB documents at write time, then simplify the Alarms Viewer to read it directly from the document instead of making a secondary API lookup.

Purpose: Eliminate the extra listProtocolConnections API call and make alarm documents self-contained with their source name.
Output: Modified AlarmThread.cs and simplified S7PlusAlarmsViewerPage.vue.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@json-scada/src/S7CommPlusClient/AlarmThread.cs
@json-scada/src/AdminUI/src/components/S7PlusAlarmsViewerPage.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add connectionName field to BuildAlarmDocument in AlarmThread.cs</name>
  <files>json-scada/src/S7CommPlusClient/AlarmThread.cs</files>
  <action>
In `BuildAlarmDocument()` (line ~238-259), add a new field `connectionName` with value `srv.name` to the BsonDocument. Place it right after the existing `connectionId` field (line 249). The new line should be:

```csharp
{ "connectionName",    srv.name ?? "" },
```

Add the null-coalescing `?? ""` for safety in case srv.name is null. Place it immediately after the `{ "connectionId", srv.protocolConnectionNumber }` line.
  </action>
  <verify>
    <automated>cd "C:/Users/tnielen/Documents/Levvel_PoC/dev" && grep -n "connectionName" json-scada/src/S7CommPlusClient/AlarmThread.cs</automated>
  </verify>
  <done>AlarmThread.cs BuildAlarmDocument includes connectionName field sourced from srv.name</done>
</task>

<task type="auto">
  <name>Task 2: Simplify Alarms Viewer to read connectionName from alarm document</name>
  <files>json-scada/src/AdminUI/src/components/S7PlusAlarmsViewerPage.vue</files>
  <action>
Make these changes to S7PlusAlarmsViewerPage.vue:

1. **Remove `connectionNameMap` ref** (line 147): Delete `const connectionNameMap = ref({})`

2. **Remove `fetchConnectionNames` function** (lines 227-241): Delete the entire async function.

3. **Remove `connectionName` helper** (line 243): Delete `const connectionName = (id) => connectionNameMap.value[id] || String(id)`

4. **Update template slot** (lines 99-101): Change from:
   ```
   <template #[`item.connectionId`]="{ item }">
     {{ connectionName(item.connectionId) }}
   </template>
   ```
   To:
   ```
   <template #[`item.connectionId`]="{ item }">
     {{ item.connectionName || item.connectionId }}
   </template>
   ```
   This provides fallback to raw connectionId for old records without connectionName.

5. **Simplify onMounted** (line 307): Change from:
   ```
   await Promise.all([fetchAlarms(), fetchConnectionNames()])
   ```
   To:
   ```
   await fetchAlarms()
   ```
  </action>
  <verify>
    <automated>cd "C:/Users/tnielen/Documents/Levvel_PoC/dev" && grep -c "fetchConnectionNames\|connectionNameMap" json-scada/src/AdminUI/src/components/S7PlusAlarmsViewerPage.vue | grep "^0$"</automated>
  </verify>
  <done>Vue component has no fetchConnectionNames or connectionNameMap references. Source column displays item.connectionName with fallback to item.connectionId. No secondary API call on page load.</done>
</task>

</tasks>

<verification>
1. `grep "connectionName" json-scada/src/S7CommPlusClient/AlarmThread.cs` shows the new field in BuildAlarmDocument
2. `grep -c "fetchConnectionNames" json-scada/src/AdminUI/src/components/S7PlusAlarmsViewerPage.vue` returns 0
3. `grep -c "connectionNameMap" json-scada/src/AdminUI/src/components/S7PlusAlarmsViewerPage.vue` returns 0
4. `grep "item.connectionName" json-scada/src/AdminUI/src/components/S7PlusAlarmsViewerPage.vue` shows the fallback pattern
</verification>

<success_criteria>
- AlarmThread.cs writes connectionName (from srv.name) into every alarm BsonDocument
- S7PlusAlarmsViewerPage.vue displays connectionName directly from alarm data
- No secondary listProtocolConnections API call remains
- Old records without connectionName gracefully fall back to showing connectionId
</success_criteria>

<output>
After completion, create `.planning/quick/260324-l5d-s7plus-alarm-mongodb-entries-include-sou/260324-l5d-01-SUMMARY.md`
</output>
